# Швидке налаштування Supabase

## Крок 1: Отримайте Anon Key

1. Відкрийте: https://jmhvlboizpvdxtxqovhe.supabase.co
2. Перейдіть: **Settings** → **API**
3. Скопіюйте **anon/public** ключ (виглядає як: `eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...`)

## Крок 2: Створіть .env.local

В корені проекту (поруч з `package.json`) створіть файл `.env.local`:

```env
NEXT_PUBLIC_SUPABASE_URL=https://jmhvlboizpvdxtxqovhe.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=вставте_ваш_ключ_тут
```

**Важливо:** 
- Замініть `вставте_ваш_ключ_тут` на реальний ключ з кроку 1
- Файл має називатися саме `.env.local` (з крапкою на початку)
- Не додавайте пробіли навколо `=`

## Крок 3: Перезапустіть сервер

Після створення `.env.local` обов'язково перезапустіть dev сервер:

1. Зупиніть поточний сервер (Ctrl+C)
2. Запустіть знову: `npm run dev`

## Крок 4: Перевірка

1. Відкрийте сайт: http://localhost:3015
2. Перейдіть: **Налаштування** → **Синхронізація даних**
3. Має з'явитися: **Статус Supabase: 🟢 Підключено**

Якщо все ще показує "Не підключено":
- Перевірте чи правильно скопійовано ключ (без пробілів)
- Переконайтеся що файл називається `.env.local` (не `.env` або `.env.local.txt`)
- Перезапустіть сервер після зміни файлу
